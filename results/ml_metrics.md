# results/ml_metrics.md

Generated by running `model/train.py` (S4 training pipeline, `docs/design_decisions.md`
D52). Metrics are on the held-out chronological validation split (4,860 rows). "With
hysteresis" numbers are `ml_golden.py`'s `hysteresis_policy()` output -- the same Schmitt-
trigger state machine `ml_policy.v` implements in RTL for gate `0x09`'s `adverse_risk`
signal; "single-threshold" numbers are the plain `z >= T_high` comparison, which is what
`model/golden_vectors.csv`'s `adverse_risk` column records and what
`tb/tb_ml_bit_exact.v` replays bit-exact through `rtl/ml_classifier_wrap.v`.

| Metric | Value |
| :-- | --: |
| Validation rows | 4,860 |
| Base (positive) rate | 9.03% (439 / 4,860) |
| Quantized classifier precision (single-threshold) | 0.457 |
| Quantized classifier recall (single-threshold) | 0.253 |
| Quantized classifier fire rate (single-threshold) | 5.00% (243 / 4,860) |
| Quantized classifier precision (with hysteresis) | 0.459 |
| Quantized classifier recall (with hysteresis) | 0.255 |
| Quantized classifier fire rate (with hysteresis) | 5.02% (244 / 4,860) |
| Quantized classifier ROC-AUC | 0.722 |
| Quantized classifier PR-AUC | 0.284 |
| Float-model ROC-AUC (diagnostic, not exported) | 0.709 |
| T_high / T_low | -454 / -458 |
| Threshold source | percentile_fallback_95 (this run's validation positive rate never reached `TARGET_PRECISION`, so `pick_thresholds` took the percentile-95 fallback branch directly -- see `docs/design_decisions.md` D52) |

**Honest caveat:** the label is a synthetic proxy (mid moved >=1 tick against the quoted
side within `LABEL_HORIZON_H` future events, `ml_engineer_brief.md` §5) on synthetic data
(`model/simulator.py`) -- these numbers demonstrate the training/quantization/export
pipeline works end to end, not real predictive power on real market microstructure.
Simulated trading only.
