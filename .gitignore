# Office lock/temp files
~$*

# hls4ml/Vitis HLS generated project (rebuilt by scripts/build_hls4ml.py per the master spec)
hls4ml/*_prj/
hls4ml/vitis_hls.log
hls4ml/.Xil/

# Vivado build artifacts
*.jou
*.log
.Xil/
*.dcp
*.bit
*.bin
*.rpt
*.rpx
*.wdb
*.vcd
*.xsa
*.ltx
*.pb
*.str
results/build/
.cache/
.hw/
.ip_user_files/
.runs/
.sim/
webtalk*.log

# generate_target output for rtl/vendor/alinx_mac/ip/*/*.xci (D26) --
# regenerated by scripts/build.tcl on every run. .xci itself is the one
# generated-but-versioned exception (see this file's own header rule).
rtl/vendor/alinx_mac/ip/**/*
!rtl/vendor/alinx_mac/ip/*/*.xci
usage_statistics_webtalk.html
usage_statistics_webtalk.xml
clockInfo.txt

# Icarus / simulation
*.vvp
sim/*.out

# Generated test stimulus (regenerable via sim/gen_soak_vectors.py)
tb/stimulus/*.mem

# Python
__pycache__/
*.pyc

# Vendor reference PDFs -- large, third-party, not versioned (PREREQUISITES.md §5)
docs/refs/*.pdf

# Nested vendor demo repo -- a real git repo, not a submodule (CLAUDE.md "Repo quirk")
docs/refs/AX7035/
